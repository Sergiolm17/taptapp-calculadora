<nav class="bg-slate-800 p-4 mb-8">
    <ul class="flex justify-center gap-8 list-none p-0 m-0">
        <li>
            <a 
                href="/" 
                class={`text-white px-4 py-2 rounded-md transition-colors hover:bg-slate-700 
                ${Astro.url.pathname === "/" ? "bg-blue-500" : ""}`}
            >
                Calculadora de Costos
            </a>
        </li>
        <li>
            <a 
                href="/imagen" 
                class={`text-white px-4 py-2 rounded-md transition-colors hover:bg-slate-700 
                ${Astro.url.pathname === "/imagen" ? "bg-blue-500" : ""}`}
            >
                Calculadora de Impresión
            </a>
        </li>
    </ul>
</nav>

<style>
    nav {
        background-color: #2c3e50;
        padding: 1rem;
        margin-bottom: 2rem;
    }

    ul {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        justify-content: center;
        gap: 2rem;
    }

    a {
        color: #fff;
        text-decoration: none;
        padding: 0.5rem 1rem;
        border-radius: 4px;
        transition: background-color 0.3s;
    }

    a:hover {
        background-color: #34495e;
    }

    .active {
        background-color: #3498db;
    }
</style> 